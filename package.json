{
  "author": "Toufiqur Rahaman Chowdhury",
  "description": "",
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "eslint": "^8.0.0",
    "lerna": "^8.2.2",
    "typescript": "^5.0.0",
    "vite": "^7.1.4",
    "vitest": "^3.2.4"
  },
  "keywords": [],
  "main": "",
  "name": "utiils",
  "license": "ISC",
  "private": true,
  "scripts": {
    "clean": "tsc --build --clean",
    "rmdist": "rm -rf packages/*/dist/*",
    "build": "tsc --build",
    "lint": "eslint . --ext .ts,.tsx",
    "publish:packages": "lerna publish",
    "start": "npm run rmdist && npm run clean && npm run watch",
    "test": "bash scripts/test.sh",
    "test:ui": "npm test :ui",
    "test:core:ui": "npm test core:ui",
    "vitest": "vitest",
    "vitest:ui": "vitest --ui",
    "vitest:coverage": "vitest --coverage",
    "watch": "tsc --build --watch"
  },
  "workspaces": [
    "packages/*"
  ],
  "version": "1.0.0"
}