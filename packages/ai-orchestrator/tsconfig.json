{
	"extends": "../../tsconfig.base.json",
	"compilerOptions": {
		"outDir": "dist",
		"rootDir": "src",
		// "baseUrl": ".", // removed for tsgo
		"paths": {
			"*": ["./src/*"]
		}
	},
	"include": ["src/**/*", "tests"],
	"exclude": ["node_modules", "dist", "**/*.test.ts"]
}
